logging:
  group:
    tomcat: org.apache.catalina, org.apache.coyote, org.apache.tomcat
  file.name: logs/application.log
  level:
    root: WARN
    tomcat: INFO
    org.springframework: DEBUG
    com.example.learnmongo: DEBUG
---

spring:
  data:
    mongodb:
      host: localhost
      database: application_db
      port: 27017
